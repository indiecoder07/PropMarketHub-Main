'use client';
import { useHomeEquityCalculator } from './useHomeEquityCalculator';
import styles from './HomeEquityCalculator.module.css';

export function HomeEquityCalculator({ className = '' }) {
  const {propertyValueInput,setPropertyValueFromInput,loanBalanceInput,setLoanBalanceFromInput,targetLvr,setTargetLvr,results,formatCurrency}=useHomeEquityCalculator();
  return <div className={[styles.wrapper,className].filter(Boolean).join(' ')}><div className={styles.grid}><section className={styles.form}><h2 className={styles.formHeading}>Home equity inputs</h2><div className={styles.field}><label className={styles.label}>Property value</label><div className={styles.inputPrefix}><span className={styles.prefix}>$</span><input className={styles.input} type="text" inputMode="numeric" value={propertyValueInput} onChange={(e)=>setPropertyValueFromInput(e.target.value)} /></div></div><div className={styles.field}><label className={styles.label}>Current loan balance</label><div className={styles.inputPrefix}><span className={styles.prefix}>$</span><input className={styles.input} type="text" inputMode="numeric" value={loanBalanceInput} onChange={(e)=>setLoanBalanceFromInput(e.target.value)} /></div></div><div className={styles.field}><div className={styles.labelRow}><label className={styles.label}>Target LVR</label><span className={styles.labelRight}>{targetLvr}%</span></div><input className={styles.slider} type="range" min={60} max={90} step={1} value={targetLvr} onChange={(e)=>setTargetLvr(Number(e.target.value))} /></div></section><section className={styles.results}>{results?<><div className={styles.primaryCard}><p className={styles.primaryLabel}>Usable equity</p><p className={styles.primaryValue}>{formatCurrency(results.usableEquity)}</p><p className={styles.primarySub}>at {targetLvr}% LVR</p></div><div className={styles.statGrid}><div className={styles.stat}><p className={styles.statValue}>{formatCurrency(results.totalEquity)}</p><p className={styles.statLabel}>Total equity</p></div><div className={styles.stat}><p className={styles.statValue}>{results.currentLvr.toFixed(1)}%</p><p className={styles.statLabel}>Current LVR</p></div><div className={styles.stat}><p className={styles.statValue}>{formatCurrency(results.maxLending)}</p><p className={styles.statLabel}>Max lending at target LVR</p></div></div></>:<p className={styles.empty}>Enter values to estimate usable equity.</p>}</section></div></div>;
}
